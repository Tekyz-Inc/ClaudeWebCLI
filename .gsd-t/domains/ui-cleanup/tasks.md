# Tasks: UI + Cleanup

## T7: Update Composer + HomePage to use unified voice hook ✅
## T8: Remove server-side dictation formatter ✅
## T9: Configure Vite for Web Worker ✅

All tasks complete. Vite handles `new URL(worker, import.meta.url)` natively — no config needed.
